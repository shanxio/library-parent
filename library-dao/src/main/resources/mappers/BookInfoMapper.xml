<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nf.library.dao.BookInfoDao">
    <sql id="bookInfoRequestMap">
        b.id, b.isbn, b.book_name, b.book_author, b.book_type,
        b.book_publish, b.book_price, b.tmamount, b.bookstock, b.book_state
    </sql>

    <delete id="bookInfoDeleteById">
        delete from bookinfo where id = #{id}
    </delete>
    <delete id="bookInfoDeleteBatch">
        delete from bookinfo
        where
        <foreach collection="array" item="isbn" separator="or">
            isbn = #{isbn}
        </foreach>
    </delete>
    <select id="getAll" resultType="com.nf.library.entity.BookInfo">
        select <include refid="bookInfoRequestMap"/> from bookinfo b order by b.id
    </select>
    <insert id="bookInfoInsert" parameterType="com.nf.library.entity.BookInfo">
        insert into
        bookinfo(isbn, book_name, book_author, book_type, book_publish, book_price, tmamount, bookstock, book_state)
        values (#{isbn},#{bookName},#{bookAuthor},#{bookType} ,#{bookPublish},#{bookPrice},
        #{tmamount},#{bookStock},#{bookState})
    </insert>
    <update id="bookInfoUpdate" parameterType="com.nf.library.entity.BookInfo">
        update bookinfo
        <set >
            <if test="isbn != null">
                isbn = #{isbn},
            </if>
            <if test="bookName != null">
                book_name = #{bookName},
            </if>
            <if test="bookAuthor !=null">
                book_author = #{bookAuthor},
            </if>
            <if test="bookType !=null">
                book_type = #{bookType},
            </if>
            <if test="bookPublish !=null">
                book_publish = #{bookPublish},
            </if>
            <if test="bookPrice !=null">
                book_price = #{bookPrice},
            </if>
            <if test="tmamount !=null">
                tmamount = #{tmamount},
            </if>
            <if test="bookStock !=null">
                bookstock = #{bookStock},
            </if>
            <if test="bookState !=null">
                book_state = #{bookState}
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>